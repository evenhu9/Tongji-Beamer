# 毕业论文答辩讲述稿
## 《基于状态机的自动化渗透测试框架设计与实现》
**总时长：约 8 分钟 | 答辩人：胡译文**

---

### 【封面页】约 15 秒

各位老师好，我是胡译文，我的毕业设计题目是《基于状态机的自动化渗透测试框架设计与实现》。下面我分四个部分汇报：研究背景、系统设计、实验评估和总结展望。

---

### 【第1页：研究背景与问题】约 60 秒

首先是研究背景。渗透测试是发现系统漏洞的核心手段，市场规模从2020年的170亿美元增长到2025年预计的450亿美元。但传统测试严重依赖人工，测试人员在Nmap、Metasploit等工具间频繁切换，流程割裂、成本高昂。大语言模型的发展使得用LLM Agent来自动化渗透测试成为研究热点。其中AutoPT是比较有代表性的工作，它设计了渗透测试状态机PSM，用五个状态——Scan、VulnSelect、Inquire、Exploit、Check——把渗透测试组织为结构化流程，约束了LLM的决策空间。

但AutoPT存在三方面缺陷：第一，PSM流程控制方面，状态粒度过粗、转移条件固定——Scan没扫到漏洞依然会空跑下游阶段；Agent提示词缺乏角色定义，出现越权调用。第二，工具体系有缺口——缺少浏览器表单交互，遇到安装向导这类需要填表点选的多步骤场景无能为力；ReAct解析器对思维链模型的非标准输出零容忍。第三，只有命令行界面，可用性受限。因此本文从这三个方向进行改进。

---

### 【第2页：PSM 渗透测试状态机】约 30 秒

这张图展示的是原版PSM的五状态模型。它是一个形式化的有限状态机，五个节点依次串联，由路由函数根据当前状态的输出决定转移路径。原版的问题是各状态按固定顺序推进，缺少条件判断——Scan扫完了不管有没有结果都会继续往下走。这正是我们第一个改进的切入点。

---

### 【第3页：系统总体架构】约 30 秒

这是改进后的四层架构。最上层Web Console提供图形化管理；业务逻辑层基于Flask API处理请求调度；核心引擎层是基于LangGraph实现的PSM引擎，是系统的大脑；基础设施层集成Nmap、Xray、Playwright等安全工具。

---

### 【第4页：改进一 — PSM 流程控制优化】约 90 秒

第一个改进是PSM流程控制。左侧是改进前后的状态转移模型对比，可以看到我们在Scan阶段之后新增了条件判断。具体来说，用parse_vuln函数对扫描结果做结构化解析——如果提取到的漏洞列表为空，就写入scan_failed标志，绕过下游的Inquire和Exploit，直接终止整条链路。这个设计处理了两种边界情况：Agent输出中出现漏洞名但缺少目标URL时，parse_vuln仍然判为无效；出现解析异常时通过try-except保守放行，避免误杀。每次提前终止可以节省大约200秒和一万五千个Token。

Scan阶段本身也细化为四个子阶段：nmap全端口扫描加服务识别、services.yml知识库匹配——精确匹配85%加上模糊匹配达到100%、xray漏洞扫描、结果评估。这套细化使Exploit Agent的工具调用次数从7.2次降到4.8次，降低33%。

PoC提取设计了四级容错——依次从Final Answer、推理文本、ReadHTML原始返回和日志回溯四个层级尝试提取。Check阶段只对工具真实输出做验证，采用LLM语义判断优先、正则通用匹配兜底的双层策略。上下文压缩方面，清洗ANSI控制符、过滤关键词、去重后截断到2000字符，再按三层结构组装。提示词方面统一了ReAct模板，禁止thinking标签、限定单句推理，并为每个Agent做了明确的角色边界和权限定义。

---

### 【第5页：改进二 — 工具扩展与容错】约 45 秒

第二个改进是工具扩展。左侧是浏览器工具层的架构图。我们基于Playwright开发了三个扩展工具：fill_element定位输入框并填入文本，select_option按值或标签选择下拉项，wait_for_selector等待元素出现并支持自定义超时——补上了原版在安装向导这类场景下的交互空白。

ReAct解析器改为六步降级策略：清洗思维链标签、标准解析、宽松正则、Action提取、模糊匹配到格式引导，把格式偏差从致命错误转为可恢复的中间状态。ReadHTML工具也做了增强，优先提取vulhub仓库中的PoC代码块，过滤掉Docker部署和环境搭建指令。

---

### 【第6页：改进三 — Web Console】约 15 秒

第三个改进是Web Console图形化管理界面，包含控制台、漏洞库、渗透测试面板、靶机管理、测试报告和系统设置六个模块，使常见操作不再依赖命令行。

---

### 【第7页：实验与评估】约 50 秒

实验方面，我们用20个真实CVE漏洞——简单和复杂各10个、3种大语言模型——GPT-4o、GPT-4o-mini和GPT-3.5-turbo、每漏洞每模型跑5次，共300组独立实验。原版数据直接取自原论文。

两个研究问题如下。RQ1模型差异：改进版总体成功率从原版24.67%提升到63.67%，提高39个百分点。GPT-4o-mini以74%成功率表现最优。值得注意的是弱模型GPT-3.5-turbo从约7%跃升至54%，增幅最大——说明流程约束可以在相当程度上补偿模型本身能力的不足。原版60组中有38组通过率为0%，改进版压缩到仅3组。RQ2成本效益：以GPT-4o-mini为例，单次成本从0.99美分降到0.31美分，降幅69%；平均耗时从161秒缩短到94秒，缩短42%。

---

### 【第8页：RQ3 改进效果评估】约 50 秒

RQ3单独看改进效果。PSM流程控制缩短了执行时间、减少了Token消耗；工具扩展效果明显——工具执行失败仅占10.1%，是五类失败原因中最低的；Web Console为300组实验提供了基础支撑。

失败原因的详细数据：提前放弃占33%，主要是重试策略没有区分失败类型；错误的命令占29.4%；上下文限制占22.9%；安全审查拦截占22%，其中GPT-4o-mini达到30.77%，说明模型安全对齐对攻击性指令的拦截率较高。工具执行失败确实是五类中最低的，10.1%。

---

### 【第9页：总结与展望】约 25 秒

总结一下。本文从PSM流程控制、工具扩展与容错、Web Console三个方向改进AutoPT。300组实验的总体成功率从24.67%提升到63.67%，成本降低69%，耗时缩短42%，原版38组零通过率压缩到仅3组。现有不足包括Agent提前放弃和安全审查拦截，未来工作将引入按失败类型区分的精细化重试策略、引入本地开源模型如DeepSeek降低对商用API的依赖、扩展PSM覆盖更多渗透测试阶段。

---

### 【致谢页】约 5 秒

以上是我的汇报，恳请各位老师批评指正，谢谢！
