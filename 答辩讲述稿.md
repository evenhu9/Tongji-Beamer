# 毕业论文答辩讲述稿
## 《基于状态机的自动化渗透测试框架设计与实现》
**总时长：约 5 分钟 | 答辩人：胡译文**

---

### 【封面页】约 15 秒

各位老师好，我是胡译文，我的毕业论文题目是《基于状态机的自动化渗透测试框架设计与实现》。下面我来汇报本文的主要内容，请老师们批评指正。

---

### 【第1页：研究背景】约 30 秒

首先是研究背景。渗透测试是发现系统漏洞的核心手段，市场规模从2020年的17亿美元增长到2025年预计的45亿美元。但传统渗透测试严重依赖人工，测试人员需要在Nmap、Metasploit等多个工具之间频繁切换，效率低下、成本高昂。

为解决这个问题，Wu等人提出了AutoPT，引入了渗透测试状态机PSM，将Scan、VulnSelect、Inquire、Exploit、Check五个阶段串联成一条自动化测试链路。本文以AutoPT为基础进行改进。

---

### 【第2页：AutoPT的局限】约 20 秒

但AutoPT在实际使用中暴露了五个明显问题：只扫默认的80端口，大量非标准端口服务被漏检；扫描无结果时仍继续执行后续阶段，浪费资源；缺少浏览器表单交互工具；ReAct解析器对思维链模型输出零容忍；以及只有命令行界面，可用性受限。

本文针对这些问题，从三个方向进行改进。

---

### 【第3页：系统总体架构】约 15 秒

这是改进后系统AutoPT+的四层架构：最上层是基于Flask和原生JavaScript实现的Web Console；中间是业务逻辑层和核心引擎层，核心引擎包含三个LLM Agent和两个程序化阶段；底层是nmap、xray、Playwright等基础工具。

---

### 【第4页：改进一 — 提示词角色差异化】约 25 秒

改进一是PSM流程控制优化，我从五个方面来说。

首先是提示词结构化。改进版将三个Agent共用的ReAct骨架抽取为统一基线模板，并为每个Agent定义了明确的角色边界：Scan Agent只能扫描、不能越权利用；Inquire Agent只有ReadHTML这一个工具，专注获取PoC；Exploit Agent则有明确的成功判定标准。这解决了原版Agent越权操作和无效尝试的问题。

---

### 【第5页：改进一 — Scan细化与提前终止】约 25 秒

其次，我将Scan阶段从原来的一步细化为四个子阶段：nmap全端口扫描、services.yml服务知识匹配、xray漏洞扫描，以及结果评估与终止判断。

尤其是第四个子阶段，当parse_vuln解析结果为空时，系统会写入scan_failed标志，直接终止整条链路，跳过Inquire和Exploit，避免了大量无效执行。

---

### 【第6页：改进一 — 上下文压缩与Check验证】约 20 秒

在上下文管理上，我实现了基于关键token过滤的即时摘要和三层结构化组装，有效控制了多轮ReAct循环中的Token增长。

Check阶段也做了改进：将匹配域从全部历史对话缩小到工具真实输出，配合LLM语义判断加正则兜底的双层校验，避免了模型推理文本被误判为利用证据的问题。

---

### 【第7页：改进二 — 工具扩展与容错解析器】约 25 秒

改进二是工具扩展与容错增强。我新增了三个浏览器扩展工具：fill_element用于表单填写，select_option用于下拉框操作，wait_for_selector用于等待动态内容加载。这三个工具补足了原版在安装向导、多步骤配置场景下的能力空白。

同时，我实现了RobustReActParser多级容错解析器，通过清洗思维链标签、标准解析、宽松正则提取等多步降级策略，将原版遇到格式偏差就直接终止的行为，改造为可恢复的容错机制。

---

### 【第8页：改进三 — Web Console】约 15 秒

改进三是Web Console图形化界面，提供控制台、漏洞库、渗透测试面板、靶机管理、测试报告和系统设置六个模块，使常见操作不再依赖命令行。

---

### 【第9页：实验设置】约 15 秒

下面汇报实验结果。我选取了20个真实CVE漏洞，分简单和复杂各10个，分别用GPT-4o、GPT-4o-mini和GPT-3.5-turbo三个模型测试，每个漏洞每个模型跑5次，共进行了300次独立实验，每次实验销毁重建Docker靶机环境。

---

### 【第10页：RQ1 模型差异分析】约 30 秒

结果显示，改进版总体成功率从原版的24.67%提升到63.67%，提高了约39个百分点。简单漏洞均值从32%升至76.67%，复杂漏洞从17.33%升至50.67%。

更值得关注的是，原版60组测试中有38组通过率为零，改进版只剩3组，这主要得益于nmap全端口扫描发现了原来漏掉的非标准端口服务。

三个模型中，GPT-4o-mini综合成功率最高，达74%。GPT-3.5的提升幅度最大，从原版的约7%跃升至54%，说明合理的流程约束可以在很大程度上弥补模型能力的不足。

---

### 【第11页：RQ2 成本效益】约 15 秒

在成本方面，以GPT-4o-mini为例，改进版的费用从0.99美元降至0.31美元，降低了69%；平均耗时从161秒缩短到94秒，节省42%。这说明流程优化本身就能带来成本收益，与此同时成功率还大幅上升。

---

### 【第12页：RQ3 改进效果评估】约 20 秒

从三个改进方向来看：PSM流程控制优化缩短了执行时间并减少了Token消耗；工具扩展与优化效果明显，工具执行失败仅占10.1%，是五类原因中最低的；Web Console提高了实验过程一致性与结果可复核性。

主要瓶颈仍是Agent提前放弃，占33%，以及模型安全审查拦截，占22%，其中GPT-4o-mini的安全审查比例达30.77%。

---

### 【第13页：总结与展望】约 20 秒

最后做个总结。本文从PSM流程控制、工具扩展与容错、Web Console三个方向对AutoPT进行了改进，总体成功率提升39个百分点，成本降低69%，耗时缩短42%。

不足之处在于Agent提前放弃和安全审查拦截的问题尚未解决，覆盖范围也仅限于Web应用层漏洞。未来计划引入精细化重试策略，并扩展PSM状态节点以覆盖更完整的渗透测试流程。

---

### 【致谢页】约 5 秒

以上是我的汇报，感谢各位老师的聆听，恳请批评指正，谢谢！

---

**备注**
- 全文约 1200 字，正常语速约 4.5-5 分钟
- 图片页（第3、5页）可适当快速带过，将时间留给改进设计和实验结果
- 实验数据部分建议背熟关键数字：63.67%、+39pp、-69%、-42%、38组→3组
