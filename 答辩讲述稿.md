# 论文答辩讲述稿

各位老师好，我是胡译文，我的毕业设计题目是《基于状态机的自动化渗透测试框架设计与实现》，指导老师钟计东老师。

---

**第一页：研究背景与问题**

渗透测试是Web安全防护的关键手段，但传统人工方式效率低、成本高。随着大语言模型的发展，用LLM Agent实现自动化渗透测试成为研究热点。AutoPT是其中的代表性工作，它用渗透测试状态机来引导Agent执行测试任务。但在实际复现中我发现，原版PSM存在三个突出问题：一是状态粒度过粗、转移条件固定、提示词缺乏规范，导致Agent容易越权和空转；二是工具体系有缺口，浏览器交互能力弱、解析器容错差；三是仅有命令行界面，可用性不足。针对这三点，我提出了对应的改进方向。

---

**第二页：PSM渗透测试状态机**

要理解改进，先看基础设计。PSM由Scan、VulnSelect、Inquire、Exploit、Check五个状态串联而成，覆盖从扫描侦察到漏洞利用再到结果验证的完整链路。它基于LangGraph实现，支持Exploit失败后回退到VulnSelect重新选择漏洞，这是它和纯线性流程最大的区别。

---

**第三页：系统总体架构**

在此基础上，系统采用四层分层架构。最底层是基础设施层，集成Nmap、Xray、Playwright等工具；核心引擎层运行LangGraph状态机和各Agent；业务逻辑层基于Flask负责任务调度；最上层是Web Console表示层。各层彼此解耦，这是后续三个改进能够独立展开的基础。

---

**第四页：改进一——PSM流程控制优化**

第一个改进围绕流程控制展开，包含六项措施。一是提前终止机制，漏洞列表为空时直接结束，避免下游空转。二是将Scan拆分为端口发现、服务匹配、漏洞扫描、结果评估四个子阶段，引入全端口nmap扫描和服务知识库，防止遗漏非标准端口。三是PoC四层级容错提取。四是优化Check验证，将匹配域缩小到真实工具输出并引入LLM判断。五是统一ReAct提示词模板，为不同Agent设定差异化的角色和权限。六是实现上下文压缩，控制Token消耗。

---

**第五页：改进二——工具扩展与容错**

第二个改进围绕工具能力展开。在浏览器交互方面，新增fill_element、select_option、wait_for_selector三个工具，补足表单填写和下拉框选择等缺失功能。在解析器方面，实现了多级降级的ReAct解析器，从清洗思维链标签、宽松正则匹配到代码块提取，逐层放宽约束。此外还增强了ReadHTML工具的PoC自动提取能力，使其从GitHub页面中精准提取利用代码而非环境部署指令。

---

**第六页：改进三——Web Console**

第三个改进是Web Console，提供控制台、漏洞库、渗透测试面板、靶机管理、测试报告和系统设置六个模块，使任务配置和结果分析不再依赖命令行。这些改进环环相扣：流程控制解决Agent空转，工具扩展补齐操作短板，界面提升可用性，三者协同提升整体效果。

---

**第七页：实验与评估**

实验方面，我选取了与原论文相同的20个CVE和三种模型，共300组独立实验。总体成功率从24.67%提升至63.67%，提升39个百分点。GPT-4o-mini表现最好达74%，而弱模型GPT-3.5-turbo从约7%跃升至54%，说明架构优化对弱模型能力不足有明显的补偿效应。成本方面，单次测试费用降低69%、耗时缩短42%，验证了流程优化能同时带来成功率和资源效率的双重改善。

---

**第八页：改进效果评估**

失败原因分析显示，工具执行失败仅占10.1%，说明工具层改进效果显著。目前主要瓶颈是Agent提前放弃占33%、错误命令占29%和安全审查拦截占22%。PSM流程优化减少了无效迭代，工具扩展降低了操作失败率，Web Console为大规模实验提供了过程支撑——三者分别对应了原版三个问题，实验数据证实了改进的有效性。

---

**第九页：总结与展望**

总结来说，本文不更换更强模型，仅通过对PSM架构内的流程控制和工具能力做针对性改进，就将成功率从约25%提升到63.67%，同时成本降低69%、耗时缩短42%。目前还有提前放弃和安全审查等局限，未来将引入精细化重试策略和DeepSeek等本地开源模型，并扩展PSM覆盖完整渗透测试阶段。

我的汇报到此结束，恳请各位老师批评指正，谢谢！
