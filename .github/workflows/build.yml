name: Build Beamer PDF

on:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - '*.md'
      - 'LICENSE'
      - '.gitignore'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Fira fonts
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y fonts-firacode
          # Install Fira Sans and Fira Math from Google Fonts / Mozilla
          mkdir -p ~/.local/share/fonts
          # Fira Sans
          curl -sL "https://github.com/mozilla/Fira/archive/refs/heads/master.tar.gz" | \
            tar xz -C /tmp --wildcards "*/ttf/FiraSans*.ttf"
          find /tmp/Fira-master/ttf -name "FiraSans*.ttf" -exec cp {} ~/.local/share/fonts/ \;
          # Fira Mono
          find /tmp/Fira-master/ttf -name "FiraMono*.ttf" -exec cp {} ~/.local/share/fonts/ \;
          # Fira Math
          curl -sLo ~/.local/share/fonts/FiraMath-Regular.otf \
            "https://github.com/firamath/firamath/releases/download/v0.3.4/FiraMath-Regular.otf"
          fc-cache -f
          echo "Installed fonts:"
          fc-list | grep -i fira

      - name: Install TeX Live
        uses: teatimeguest/setup-texlive-action@v3
        with:
          packages: >-
            scheme-basic
            beamer
            biblatex
            biblatex-gb7714-2015
            biber
            booktabs
            caption
            ctex
            fandol
            fontspec
            latexmk
            pgf
            realscripts
            unicode-math
            xecjk
            xetex
            collection-fontsrecommended

      - name: Compile Beamer presentation
        run: |
          latexmk -xelatex -halt-on-error -shell-escape -interaction=nonstopmode main.tex

      - name: Show log on failure
        if: failure()
        run: |
          echo "=== main.log ==="
          cat main.log || true

      - name: Upload PDF
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: tongji-beamer-defense-ppt
          path: main.pdf
          retention-days: 90
